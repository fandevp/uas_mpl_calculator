import 'package:flutter/material.dart';

class VibrationOnTouchPage extends StatelessWidget {
  const VibrationOnTouchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Vibration on Touch')),
      body: const Center(child: Text('Vibration on Touch Page')),
    );
  }
}

class MemoryButtonsPage extends StatelessWidget {
  const MemoryButtonsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Memory Buttons')),
      body: const Center(child: Text('Memory Buttons Page')),
    );
  }
}

class KeypadLayoutPage extends StatelessWidget {
  const KeypadLayoutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Keypad Layout')),
      body: const Center(child: Text('Keypad Layout Page')),
    );
  }
}

class ThemePage extends StatelessWidget {
  const ThemePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Theme')),
      body: const Center(child: Text('Theme Page')),
    );
  }
}

class DisplayFormatPage extends StatelessWidget {
  const DisplayFormatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Display Format')),
      body: const Center(child: Text('Display Format Page')),
    );
  }
}

class ShareAppPage extends StatelessWidget {
  const ShareAppPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Share this App')),
      body: const Center(child: Text('Share this App Page')),
    );
  }
}

class SendFeedbackPage extends StatelessWidget {
  const SendFeedbackPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Send Feedback')),
      body: const Center(child: Text('Send Feedback Page')),
    );
  }
}

class RateAppPage extends StatelessWidget {
  const RateAppPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rate this App')),
      body: const Center(child: Text('Rate this App Page')),
    );
  }
}

class UpgradePage extends StatelessWidget {
  const UpgradePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Upgrade (Ad-Free Version)')),
      body: const Center(child: Text('Upgrade (Ad-Free Version) Page')),
    );
  }
}

class OtherAppPage extends StatelessWidget {
  const OtherAppPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Other App')),
      body: const Center(child: Text('Other App Page')),
    );
  }
}

class AboutPage extends StatelessWidget {
  const AboutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('About')),
      body: const Center(child: Text('About Page')),
    );
  }
}
